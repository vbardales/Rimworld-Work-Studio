# 2026-09-23

Runs of the Work Studio suite on the WSL install. Mod DLL under test for all five: `bb48d04`
(2026-09-22 22:47), steps DLL `9849785`. Every report was read before being cited: `setName`, suite
and scenario names, and the launcher's own totals agreed. Media are on disk in
`Tests/Pickle/evidence/2026-09-23-<name>/`, ignored by git; the shared archive copies carry a
`keep.txt` (`pickle-reports-archive/0923-1835`, `0923-1843`, `0923-1851`).

## Scenario 5 alone, `avec-enhanced-work-tab` - passed

`05-priorities-across-save.feature`: 3 discovered, 3 passed, 0 failed, 0 skipped, `exitReason: passed`.
Confirms the fix of `ed41491` (a created type's defName only goes up): "a save written with one type,
loaded with two" reads 0 for the type absent from the save with Enhanced Work Tab loaded. A first
claim of this result on 2026-09-22 is withdrawn: that report belonged to an unrelated PickleTools test.
Media: `evidence/2026-09-23-ewt-scenario5/`.

## Scenario 2 alone, `incompat-fluffy-worktab` - failed

`02-work-tab-button.feature`: 3 discovered, 2 passed, 1 failed. "Work types… opens the editor" failed:
no `Dialog_WorkTypes` in 60 frames. Measured: the pointer stayed at (1058, 774) before and after the
click, the old narrow-window position; the probe saw the button at (1754, 759, 155x28), centre
(1831.5, 773); the wait for the button to stand still and the reachability check had passed; the window
stack was clean. **Cause not established**: the layout-race explanation covers the first failure, not this
one. Candidates, none checked: a stale or duplicated tag in Pickle, or the hover step. The capture (opened)
shows the button at the far right with the pointer over column headers.
Media: `evidence/2026-09-23-fluffy-scenario2/` (with the capture).

## Full pass, `avec-enhanced-work-tab`, English - passed

55 discovered, 49 passed, 0 failed, 6 skipped, `exitReason: passed`. The 6 skips are the `@requires`
scenarios of mods this set does not stage: Fluffy Work Tab (feature 15), RIMMSQOL (feature 16, four),
Work Type Tag (feature 17). Under this mod the Work types button scenario and both save scenarios of
feature 05 pass, so the leftward placement past Enhanced Work Tab's "Done editing" works.
Captures: only the Workshop editor one opened with the owner. It shows the two "Carry to growth vat"
rows told apart by their defNames, but cut mid-word (`CarryToG...`), which is why the row now puts the
defName on its own line (`c29297d`); and the background is the test fixture's desert, not the showcase
colony, so it cannot be the Workshop image (the feature now loads `Nelim-Zen-Meadow-Studio`). Other
captures of this pass: not yet reviewed.
Media: `evidence/2026-09-23-ewt-full/` (summary only; the pass is in the archive `0923-1835`).

## Full pass, `sans-facultatifs`, English - passed

55 discovered, 49 passed, 0 failed, 6 skipped (same six). Captures: not yet reviewed.
Media: archive `0923-1843`; summary in `evidence/2026-09-23-minimal-english/`.

## Full pass, `sans-facultatifs`, French - passed

55 discovered, 49 passed, 0 failed, 6 skipped (same six), launcher reported `langue : French`.
Captures: not yet reviewed. Media: archive `0923-1851`; summary in `evidence/2026-09-23-minimal-french/`.

## Still owed for the tested gate

Fluffy Work Tab full set (feature 15), RIMMSQOL (16), Work Type Tag (17), Better Work Tab and Compact
Work Tab sets, and the Workshop captures on `Nelim-Zen-Meadow-Studio`: all queued on the build with the
defName-on-its-own-line change (`c29297d`/`3dcfdb1`), which none of the five runs above contains.
