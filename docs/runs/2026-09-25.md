# 2026-09-25

## `the new column in the Work tab`, developer mode off - passed

Filed through the TicketDispatcher (`20260925-113457-632-c33d`), exit 0, 1 scenario, 1 passed, 17 s, `setName studio`,
English, `wsl-deps.studio.map`. The scenario now runs `Nelim's Pickle Tools: developer mode is turned off for the capture`
(ScreenshotMode, PickleTools `2f530b6`) before the shot: the runner starts the game with developer mode on, and the
2026-09-24 image carried its toolbar along the top. The new image was opened: the toolbar is gone, the tab bar and the
column `Tidying` are there. It replaces `Art/Workshop/01-the-new-column.jpg` (JPEG q95, 0.74 MB). The report was reduced
to its text files (`Tests/Pickle/evidence/0925-worktab-shot-nodev`, ignored by git).

Still in the dispatcher's queue for this version, published as 1.2.0 (fail-fast policy, `PUBLICATION.md`): the Work types
button under Fluffy Work Tab (`02-work-tab-button.feature`, `20260925-113502-435-10d5`) and the Enhanced Work Tab full
pass (`20260925-113506-107-57e1`).

## Enhanced Work Tab, full pass - passed

`20260925-113506-107-57e1`, exit 0, `setName avec-enhanced-work-tab`, English, 55 scenarios: 46 passed, 0 failed,
9 skipped (the `@requires` ones of other mods: 3 publication shots, the Fluffy conflict, 4 RIMMSQOL, 1 Work Type Tag; they
belong to other passes). The scenarios that had failed under this mod on 2026-09-23 pass: `Work types… opens the editor`
(the button clear of Enhanced Work Tab's "Done editing"), and the three scenarios of `priorities survive a reconfiguration
across a save`. Build under test: the one published as 1.2.0 (`7a9ffec`). Reduced to its text files and the 9 captures of
this mod's `@review` scenarios (`Tests/Pickle/evidence/0925-ewt-full`, ignored by git); the captures are not yet opened.

## Fluffy Work Tab, the button - not run

`20260925-113502-435-10d5`, exit 1, no report: the launcher did not find Fluffy's Work Tab (3552152340) in any Workshop
folder and stopped before launching the game. Nothing was tested; it is not a red result. Needs the mod downloaded
(`scripts/download-workshop-wsl.sh 3552152340`, the owner's word) and the request filed again.

## Fluffy Work Tab, the button - failed again, same shape as 2026-09-23

`20260925-175038-375-6582` (label carries `aec41eb`), `incompat-fluffy-worktab`, English, `02-work-tab-button.feature`:
3 scenarios, 2 passed (the Animals and Assign tabs get no button), 1 failed, `setName incompat-fluffy-worktab`, exitReason
`failed`, 21 s for the failing one. Fluffy's Work Tab was installed for it the same day (`download-workshop-wsl.sh`, the earlier
request having stopped on its absence).

`Work types… opens the editor`: "the click ... reached no window named Dialog_WorkTypes in 60 frames". Measured, as printed
by the step: the pointer was at (1058, 774) before AND after the click; the button was drawn at x 1754 to 1909, y 759 to 787,
centre (1831.5, 773); no drawn button contains the pointer; the stack is `MainTabWindow_WorkTab [WorkTab]` (0, 750, 1920 x 295,
holds the pointer), the learning readout and the debug-window opener, none absorbing input. (1058, 774) is where the button
was drawn while the tab window was still narrow (2026-09-23 record above). Not established: why the click step resolved that
position after 12 frames of the button standing still. It is the same failure as on 2026-09-21 and 2026-09-23, so it is not a
regression of 1.2.0: it predates the publication, and it is under a mod that `About.xml` declares incompatible.

Evidence: `Tests/Pickle/evidence/0925-fluffy-button-2` (verdict files and the failure's screenshots; log, ndjson and html deleted).

## Fluffy Work Tab, the button - diagnosed, fix built

Three more requests (`ed01`, `873c`, and one cancelled). The first did not print what it was meant to (my replacement of the
message text had not landed; found by reading its output). With the longer wait (60 frames and two seconds, PickleTools
`d938c8c`) and Pickle's tag store printed, `873c` gave: the store held `Work types…` at x 981 (the narrow layout) at frames 283
and 285, `Duplicate = False`; the pointer was at (1058, 774), on the button. The capture taken just before the click shows the
tab window narrow (ending near x 1145) with few columns, and the capture at the end of the failure shows it wide with about
fifty: the table had been expanded. Fluffy's Work Tab has an "Expand all priorities" toggle in the same corner (its tooltip
is on the first capture of the earlier run). Reading: the click reached that toggle, not our button, which is drawn over it;
the toggle expanded the table and widened the window. This fits the failure of 2026-09-21 and 2026-09-23, and it means the
first explanation of that day (a Work Tab toggle took the click) was closer than the two that replaced it; it is a reading of
those captures and the tooltip, not a measurement of which control received the press.

Fix: with `fluffy.worktab` active the button is placed 170 px further left (`Patch_WorkTabButton`, built, off-game suite
0 failed, 1 skipped). To be replayed: `02-work-tab-button.feature` under `incompat-fluffy-worktab`.
