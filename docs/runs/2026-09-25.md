# 2026-09-25

## `the new column in the Work tab`, developer mode off - passed

Filed through the TicketDispatcher (`20260925-113457-632-c33d`), exit 0, 1 scenario, 1 passed, 17 s, `setName studio`,
English, `wsl-deps.studio.map`. The scenario now runs `Nelim's Pickle Tools: developer mode is turned off for the capture`
(ScreenshotMode, PickleTools `2f530b6`) before the shot: the runner starts the game with developer mode on, and the
2026-09-24 image carried its toolbar along the top. The new image was opened: the toolbar is gone, the tab bar and the
column `Tidying` are there. It replaces `Art/Workshop/01-the-new-column.jpg` (JPEG q95, 0.74 MB). The report was reduced
to its text files (`Tests/Pickle/evidence/0925-worktab-shot-nodev`, ignored by git).

Still in the dispatcher's queue for this version, published as 1.2.0 (fail-fast policy, `PUBLICATION.md`): the Work types
button under Fluffy Work Tab (`02-work-tab-button.feature`, `20260925-113502-435-10d5`) and the Enhanced Work Tab full
pass (`20260925-113506-107-57e1`).

## Enhanced Work Tab, full pass - passed

`20260925-113506-107-57e1`, exit 0, `setName avec-enhanced-work-tab`, English, 55 scenarios: 46 passed, 0 failed,
9 skipped (the `@requires` ones of other mods: 3 publication shots, the Fluffy conflict, 4 RIMMSQOL, 1 Work Type Tag; they
belong to other passes). The scenarios that had failed under this mod on 2026-09-23 pass: `Work types… opens the editor`
(the button clear of Enhanced Work Tab's "Done editing"), and the three scenarios of `priorities survive a reconfiguration
across a save`. Build under test: the one published as 1.2.0 (`7a9ffec`). Reduced to its text files and the 9 captures of
this mod's `@review` scenarios (`Tests/Pickle/evidence/0925-ewt-full`, ignored by git); the captures are not yet opened.

## Fluffy Work Tab, the button - not run

`20260925-113502-435-10d5`, exit 1, no report: the launcher did not find Fluffy's Work Tab (3552152340) in any Workshop
folder and stopped before launching the game. Nothing was tested; it is not a red result. Needs the mod downloaded
(`scripts/download-workshop-wsl.sh 3552152340`, the owner's word) and the request filed again.
