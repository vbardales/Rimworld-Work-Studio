# 2026-09-22

Full passes run by another session, before the 22:47 fix (`bb48d04`). The report of each was copied
to `Tests/Pickle/evidence/2026-09-22-<name>/` (ignored by git) before the shared lock was released.
**Deleted on 2026-09-23** to free disk (project rule: only the latest report per set for the current revision is kept): enhanced work tab, work type tag, minimal English and minimal French were replaced by the runs of `2026-09-23.md`; only Better Work Tab's, which has no newer run yet, is still on disk. What they showed is in the table and text below. The
revision tested was not written down at the time; it is earlier than `bb48d04`. Captures: opened by that
session, not reviewed with the owner.

| Run | Set | Language | Discovered / passed / failed / skipped | `exitReason` |
| --- | --- | --- | --- | --- |
| minimal English | `sans-facultatifs` | English | 55 / 49 / 0 / 6 | passed |
| minimal French | `sans-facultatifs` | French | 55 / 49 / 0 / 6 | passed |
| Better Work Tab | `avec-better-work-tab` | English | 55 / 49 / 0 / 6 | passed |
| Work Type Tag | `avec-work-type-tag` | English | 55 / 50 / 0 / 5 | passed |
| Enhanced Work Tab | `avec-enhanced-work-tab` | English | 55 / 48 / 1 / 6 | **failed** |

## The failure: Enhanced Work Tab took the click

"Work types… opens the editor" failed with the pointer at exactly the centre of the drawn button, (1389,
707). A text button drawn earlier, "Done editing" (98x24 at 1362, 697; drawn #64 against #103 for ours),
contained that point, in a Work window that is vanilla `MainTabWindow_Work` here, so that mod patches the
tab rather than replaces it. Cause: that mod's own button sits over the Work types button at the top
right. Fix: a mod-specific leftward placement of our button (`bb48d04`), confirmed by the full pass of
2026-09-23 (`2026-09-23.md`). This run is kept as a failed regression and was never counted as tested.
