# 2026-09-26 - final-build passes on WorkStudio 16a99bb

| Pass | Result | Evidence |
|---|---|---|
| Fluffy button replay (591d) | green 3/3 | `0925-fluffy-button-5` |
| minimal English (1c2f) | 45 passed, 1 failed, 9 skipped: "confirming the reset clears the configuration", step "click ResetAll" timed out after 5 s | `0926-min-en` |
| minimal French (5012) | 46 passed, 0 failed, 9 skipped | `0926-min-fr` |
| Better Work Tab (9f60) | 45 passed, 1 failed, 9 skipped: this time "the reset button asks before it destroys anything", same step timing out | `0926-better-work-tab` |

Reading so far: the red moves between two scenarios of `13-settings.feature` that share the same first click (the reset button,
drawn only when there is something to reset; the capture of 1c2f shows it absent), both directly after the scenario that
re-reads the configuration from disk; the French pass and the other scenarios of the same feature pass. Not shown to be a
regression (only the Work types button code changed since the green 2026-09-23 pass); not shown to be timing either. One-scenario
replay `01d4` is queued.
| Compact Work Tab (2c9f) | 45 passed, 1 failed, 9 skipped: "the reset button asks before it destroys anything", same step | `0926-incompat-compact-worktab` |

Third occurrence of the same step (1c2f, 9f60, 2c9f), on three English-language sets; the French pass is green. Waiting for the isolated
replay `01d4` before reading anything into the order of the scenarios.
| RIMMSQOL (e0fc) | 49 passed, 1 failed, 5 skipped: "a save written with two types, loaded with one" (feature 05), `Log.Error` ArgumentOutOfRangeException from `RIMMSqol.DefMapSaveStateFix.Postfix` | `0926-rimmsqol` |

The RIMMSQOL red is the one first seen on 2026-09-23 (same scenario, same stack, a priorities node of 25 values read by RIMMSQOL's postfix
on `DefMap.ExposeData`), so it is not new and not a regression of 1.2.0. The `13-settings` red did not occur in this pass. Still open: whether
Work Studio can avoid it or it is RIMMSQOL's to fix, and whether a player loses priorities (never checked).
