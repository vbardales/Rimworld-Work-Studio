# 2026-09-24

Workshop captures on the owner's showcase colony (`nelim-zen-meadow-studio` from PickleTools/ScreenshotStudio),
`wsl-deps.studio.map`, English. DLL under test: `3dcfdb1` and after. Every report was read before being cited: `setName`
and scenario names agree with what was asked.

## `14-publication-shots.feature`, first run - 2 passed, 1 failed

`setName studio`, 3 scenarios. The editor and the settings shots passed. The Work tab scenario failed: the step
`I open the "Work" tab` took 10.5 s on this heavy colony and died on Pickle's five-second step budget, though the tab was
open in the failure image. The run was then killed by the launcher's stall guard (no output for 5 minutes) after the game
had already forced its own exit, which is why the launcher reported 3 and the report says `in-progress`.
Fix: `@timeout:30` on that scenario (`f947178`).

## `the new column in the Work tab` alone - passed

Filed through the TicketDispatcher (`20260924-204534-438-00e4`), exit 0, 1 scenario, 1 passed, 15 s, `setName studio`.
Two requests came first: my direct launcher died with its session after 90 minutes in the queue (code 7), and a request
filtered by the scenario's bare name matched nothing (`infrastructure-error`, 0 played): a scenario is targeted with
`-Filter '::<scenario name>'`, a bare phrase is read as the mod's display name.

## Kept

The three finals are in `Art/Workshop/ready/` (see `PUBLICATION.md`); the reports were reduced to their text files
(`Tests/Pickle/evidence/0924-worktab-shot-2`, ignored by git). The raw captures were deleted after the crop.
