# Normalise line endings in the repository, while each machine keeps its own.
* text=auto

*.cs   text diff=csharp
*.xml  text
*.md   text
*.props text
*.yml  text
Tests/Pickle/*.map text eol=lf

# Binaries: neither normalised nor diffed as text.
*.png binary
*.jpg binary
*.dll binary
