# Verification — 2026-09-17

Revision: `a60f87e` (the MainButtons shortcut commit) plus `Source/AccessChecks.cs`, added the
same day after this suite caught its absence. No in-game test was performed.

Commands:

```powershell
dotnet build Source/WorkStudio.csproj -c Release
dotnet build Tests/OffGame/WorkStudio.Tests.csproj -c Release
.build/offgame/bin/Release/net48/WorkStudio.Tests.exe
../scripts/Check-DefInjected.ps1 -TransMod ./Mod
```

Both builds: zero warnings/errors. Tests against the freshly built DLL: **26 PASS, 0 FAIL, 1
SKIP**. DefInjected (run separately, see the settings audit in STATUS.md): 2 keys checked, 0
errors.

## What was actually caught

This suite exists because of one real defect, found before it existed: `Source/WorkStudio.csproj`
declares `<Publicize Include="Assembly-CSharp" />` and `<GenerateAssemblyInfo>false</GenerateAssemblyInfo>`
together, the exact combination that silently dropped `Contented Livestock`'s waiver
(`rimworld-tests-hors-jeu`, "the mod publicises" chapter). `WorkStudio.dll` had the same hole:
the `IgnoresAccessChecksToAttribute` type was embedded but never applied. `PriorityMemory.Restore`
reads `Pawn_WorkSettings.priorities` and writes its `workGiversDirty`, both private in the real
game, on every `Apply()` — meaning every startup and every edit. Left alone, this was a
`FieldAccessException` waiting on the desktop CLR the first time any pawn's priorities were
restored; whether Mono in the shipped game enforces the same check was never established either
way in this codebase (see the same memory chapter's "severity not established" note), which is a
reason to fix it, not a reason to leave it.

Confirmed by mutation: removing `Source/AccessChecks.cs` and rebuilding turns two checks red with
a real `FieldAccessException` thrown from inside `PriorityMemory.Restore`. Restoring the file and
rebuilding turns them green again. Both runs are reproducible from a clean tree.

## The full scope, found by a real IL scan rather than assumed

A second check (`TheNonPublicMemberScan`) walks every method body the shipped DLL contains,
resolves every field/method token it references, and flags whichever belong to `Assembly-CSharp`
and are not public — the "balayage inverse" from `rimworld-tests-hors-jeu`. Manual reading of the
source had found four private members before this scan existed
(`Pawn_WorkSettings.priorities`/`workGiversDirty`/`CacheWorkGiversInOrder`,
`PawnColumnDef.workerInt`, `BackstoryDef.cachedDisabledWorkTypes`). The scan found **three more**,
all real and all already covered by the same one-line fix:

- `GenFilePaths.FolderUnderSaveData` — called directly by `ConfigFile.Folder`.
- `DefDatabase<T>.Remove` — called by `WorkTypeRuntime.SyncCustomTypes` to retire a deleted custom
  work type's `WorkTypeDef` and `PawnColumnDef`.
- `Pawn_WorkSettings.pawn` — read by `Patch_WorkSettingsExposeData.Restore` to notify the pawn
  after loading named priorities back.

Two more members are touched and correctly *not* flagged: `MainButtonWorker..ctor` and
`ModSettings..ctor`, both `protected` and reached only from a subclass declared in this mod
(`MainButtonWorker_OpenSettings`, `WorkStudioSettings`) — ordinary legal C# that needs no waiver at
all, per the same memory chapter's exemption rule. The check itself asserts the conditional form
that chapter settles on: finding non-public members here is expected, since the mod deliberately
uses the Publicizer; failing would mean finding them *without* the assembly declaring the waiver.

## What stays untested here

- `ConfigFile.PathFor`/`Folder`/`Export`/`Import` and `MainButtonWorker.Visible`'s real getter:
  all reach `GenFilePaths.SaveDataFolderPath`, and merely JIT-compiling that property throws
  outside a running Unity player (`ModsConfig`'s static constructor hits the same wall). Only the
  serialization contract these wrap — `WorkStudioSettings.ExposeConfig()` — is exercised directly,
  against a path this suite computes itself.
- Everything `Tests/Pickle/` covers or defers to a manual pass in game: see its own README and
  TESTING.md.

## Output, Keyed coverage collapsed to a count

The real run lists all 63 keys one by one; kept as a count here since each line reads the same
("carries the same {N} placeholders in both languages").

```
RimWorld:      C:\Program Files (x86)\Steam\steamapps\common\RimWorld\RimWorldWin64_Data\Managed
mod assembly:  <repo>\Tests\OffGame\..\..\Mod\Assemblies\WorkStudio.dll
mod folder:    <repo>\Tests\OffGame\..\..\Mod

the publicizer waiver:
  PASS  WorkStudio.dll declares [assembly: IgnoresAccessChecksTo("Assembly-CSharp")]

every non-public Assembly-CSharp member the shipped DLL touches:
    BackstoryDef.cachedDisabledWorkTypes
    DefDatabase`1.Remove
    DefMap`2.values
    GenFilePaths.FolderUnderSaveData
    MainButtonWorker..ctor  (protected, reached from a subclass - no waiver needed)
    ModSettings..ctor  (protected, reached from a subclass - no waiver needed)
    Pawn_WorkSettings.CacheWorkGiversInOrder
    Pawn_WorkSettings.pawn
    Pawn_WorkSettings.priorities
    Pawn_WorkSettings.workGiversDirty
    PawnColumnDef.workerInt
    (11 token(s) left unresolved - generic instantiations, not a game reference)
  PASS  9 non-public member(s) touched (BackstoryDef.cachedDisabledWorkTypes, DefDatabase`1.Remove, DefMap`2.values, GenFilePaths.FolderUnderSaveData, Pawn_WorkSettings.CacheWorkGiversInOrder, Pawn_WorkSettings.pawn, Pawn_WorkSettings.priorities, Pawn_WorkSettings.workGiversDirty, PawnColumnDef.workerInt), all legal only because the assembly declares IgnoresAccessChecksTo

the private Assembly-CSharp members PriorityMemory touches, exercised for real:
  PASS  settings.workGiversDirty was written
  PASS  settings.CacheWorkGiversInOrder() ran without an access exception

the Harmony patch targets still exist in the installed game:
  PASS  Pawn.GetDisabledWorkTypes() still exists
  PASS  Pawn_WorkSettings.ExposeData() still exists
  PASS  FindDrawMethod(typeof(MainTabWindow_Work)) finds a DoWindowContents to patch

the hidden MainButtons shortcut:
  PASS  WorkStudio_Settings is declared
  PASS  hidden by default (buttonVisible=false)
  PASS  workerClass names WorkStudio.MainButtonWorker_OpenSettings
  PASS  MainButtonWorker_OpenSettings derives from MainButtonWorker
  PASS  Activate() is declared on the worker itself
  PASS  Activate() overrides MainButtonWorker.Activate rather than hiding it
  SKIP  Worker.Visible: unreachable off-game - ModsConfig.IdeologyActive's static constructor needs GenFilePaths.SaveDataFolderPath, which cannot be called at all in this harness

PriorityMemory's fallback chain:
  PASS  a saved value for the type itself wins
  PASS  no saved value for the type, but a seed that has one, takes the seed's value
  PASS  no saved value and no useful seed falls back to Pawn_WorkSettings.DefaultPriority
  PASS  a pawn Work Studio never met (a null saved dictionary) also falls back to the default

WorkStudioSettings' Scribe round trip (ConfigFile itself needs GenFilePaths, see remaining):
  PASS  the exported file exists
  PASS  the export carries the custom type
  PASS  the export carries the hidden type
  PASS  the export does NOT carry knownWorkTypes, a snapshot of this machine's mod list
  PASS  the loaded settings carry one custom type back
  PASS  the loaded settings carry the hidden type back

Keyed coverage, English against French:
  PASS  the English Keyed file exists
  PASS  the French Keyed file exists
  PASS  no key exists in English only
  PASS  no key exists in French only
  PASS  no English value is empty
  PASS  no French value is empty
  PASS  63 keys carry the same {N} placeholders in both languages

0 CHECK(S) FAILED; 1 CHECK(S) INCOMPLETE
1 check(s) skipped
```

Reproduced on the same revision, exact 26/0/1 result.
